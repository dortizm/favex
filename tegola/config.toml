[webserver] 
port = ":9090" 

[[providers]]
name = "pg"
type = "postgis"
uri = "postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}?sslmode=disable"
srid = 3857

[[maps]] 
name = "${TEGOLA_MAP_NAME}" 
attribution = "Demo" 

[[maps.layers]] 
name = "hex5km" 
provider_layer = "pg.hex5km" 
min_zoom = "${TEGOLA_MIN_ZOOM}" 
max_zoom = "${TEGOLA_MAX_ZOOM}" 

[[providers.layers]]
name = "hex5km"
geometry_fieldname = "geom"
id_fieldname = "gid"
geometry_type = "multipolygon"

sql = """
SELECT
  gid,
  hex_id,
  sr,
  ST_AsBinary(ST_Boundary(geom_3857)) AS geom
FROM public.hex5km
WHERE geom_3857 && !BBOX!;
"""